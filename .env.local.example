NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Required for sync (bypasses RLS for opportunities upsert)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Required for Vercel Cron to call /api/opportunities/sync
CRON_SECRET=your-cron-secret
# Optional: for AI-based major tagging during sync (keyword fallback used if unset)
OPENAI_API_KEY=
# Groq: opportunity summarization and resume-based matching
GROQ_API_KEY=
